export type ScaleOptions = {
  minValue?: number;
  maxValue?: number;
};

export type SizeOptions = {
  width?: number;
  height?: number;
};

export type StrokeOptions = {
  strokeColor?: string;
  strokeWidth?: number;
};
